# Spar language conformance corpus

These fixtures are the canonical syntax examples shared by the compiler,
tree-sitter, TextMate grammar checks, and documentation builds. Each consumer
must parse `manifest.json` and validate every listed source file.
